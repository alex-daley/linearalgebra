package com.maths.linearalgebra;

/**
 * Represent a 2D column vector (defines a magnitude/length and a direction).
 * Think of this as an arrow starting at the origin on a 2D cartesian grid (or at the tip of another vector on a grid).
 */
public record Vector2(double x, double y) { }
